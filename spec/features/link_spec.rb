require 'rails_helper'

RSpec.describe "link creation/deletion", type: :feature do

  it "creates links" do
    # visit '/sessions/new'
    # within("#session") do
    #   fill_in 'Email', with: 'user@example.com'
    #   fill_in 'Password', with: 'password'
    # end
    # click_button 'Sign in'
    # expect(page).to have_content 'Success'
  end
end
